# boaz-go-project-helper

This tool helps build go projects easier.

### create new package
```sh
# create new package with version into GOPATH
go create [pkgName] [version]
ex:
$ go create testpkg 1.0
```